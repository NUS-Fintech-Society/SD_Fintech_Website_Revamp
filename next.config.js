/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fintechsociety.comp.nus.edu.sg', 'images.unsplash.com']
  }
};

module.exports = nextConfig;
